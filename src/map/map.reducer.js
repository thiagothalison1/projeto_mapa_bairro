const defaultState = {
    defaultZoom: 14,
    latlong: {
        lat : -26.9889496,
        lng : -48.6300796
    }
}

const map = (state = defaultState, action) => {
    switch (action.type) {
      default:
        return state
    }
}
  
export default map